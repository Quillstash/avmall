{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "pnpm run check:boundaries && pnpm exec prisma migrate deploy && pnpm run build",
  "crons": [
    {
      "path": "/api/cron/sales-summary",
      "schedule": "0 5 * * *"
    }
  ]
}
